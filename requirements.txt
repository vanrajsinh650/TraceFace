# Core face detection and embedding
insightface>=0.7.3
onnxruntime>=1.15.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=9.5.0

# Async HTTP (PimEyes API + candidate image download)
httpx>=0.27.0

# Reverse image search (fallback — Google, Yandex, Bing)
# Note: PicImageSearch requires lxml>=5.4.0,<6.0.0 and pyquery
lxml>=5.4.0,<6.0.0
pyquery>=2.1.0
cssselect>=1.5.0
PicImageSearch>=3.8.0

# Blockchain (Ethereum Sepolia via web3.py)
web3>=6.0.0

# Environment / config
python-dotenv>=1.0.0

# Optional: faster cosine similarity (from eye_of_web)
# numba>=0.57.0

# Optional: GPU inference (uncomment if CUDA available)
# onnxruntime-gpu>=1.15.0
